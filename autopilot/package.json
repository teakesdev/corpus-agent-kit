{
  "name": "@corpus-agent-kit/autopilot",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc && cp src/eval/scenarios.json dist/eval/",
    "eval": "tsc && node dist/eval/smoke.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/server.js",
    "dev": "tsc && node dist/server.js"
  },
  "dependencies": { "openai": "^4.70.0" },
  "devDependencies": { "typescript": "^5.6.3", "@types/node": "^22.0.0", "vitest": "^2.1.0" }
}
