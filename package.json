{
  "name": "corpus-agent-kit",
  "private": true,
  "version": "0.1.0",
  "license": "Apache-2.0",
  "workspaces": ["autopilot", "mcp-server", "widget"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check:plugin-skill-drift": "bash scripts/check-plugin-skill-drift.sh",
    "test": "npm run check:plugin-skill-drift && npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
